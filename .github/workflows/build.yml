on: [push, pull_request, workflow_dispatch]

name: Build

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: zmkfirmware/zmk-build-arm:2.5
    name: Build PNCATEHO right
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache west modules
        uses: actions/cache@v4
        env:
          cache-name: cache-zephyr-modules
        with:
          path: |
            modules/
            tools/
            zephyr/
            bootloader/
            zmk/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('manifest-dir/west.yml') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-

      - name: West Init
        run: west init -l config

      - name: West Update
        run: west update

      - name: West Zephyr export
        run: west zephyr-export

      - name: West Build (PNCATEHO right)
        run: west build -s zmk/app -b promicro_nrf52840 -- -DSHIELD=PNCATEHO_right -DZMK_CONFIG="${GITHUB_WORKSPACE}/config"

      - name: PNCATEHO right DTS File
        if: ${{ always() }}
        run: cat -n build/zephyr/promicro_nrf52840.dts.pre.tmp

      - name: PNCATEHO right Kconfig file
        run: cat build/zephyr/.config | grep -v "^#" | grep -v "^$"

      - name: Rename zmk.uf2
        run: cp build/zephyr/zmk.uf2 PNCATEHO_right_promicro_nrf52840.uf2

      - name: Archive (PNCATEHO right)
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: PNCATEHO_right_promicro_nrf52840.uf2
